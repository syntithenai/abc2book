# Local media resolver base URL (no trailing slash).
# Run: cd local-resolver && docker compose up --build
# Users must be logged in with Google; email must be on the resolver allowlist.
REACT_APP_MEDIA_PROXY_BASE=http://localhost:8787
