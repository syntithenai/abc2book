import abcjs from "abcjs";
import {useState, useEffect, cloneElement} from 'react'
import {Button} from 'react-bootstrap'
export default function AbcAudio(props) {
    //console.log(props)
    
    
    
    //return <div onClick={startInitButton} >
        //{buttons}
        //{props.children ? cloneElement(props.children, Object.assign({},props,{createPlayer}))  : null}
    //</div>
    
  return null
}
