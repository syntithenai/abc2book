/** Re-enable to show Practice mode (/practice) in nav and routes. */
export const PRACTICE_MODE_ENABLED = false

/** Re-enable to show Practice lists in nav, bulk ops, and routes. */
export const PRACTICE_LISTS_ENABLED = false
